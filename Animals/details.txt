1. A program that defines a class person, the class constructor method, 3 private properties (age, height, name), then create 3 instances (objects) of that class.

// Assignment 1
2. Write a program that models your favorite animal and creates 4 objects of that animal, also create additional two functions that is only unique to that animal.

